apiVersion: v1
kind: Service
metadata:
  name: {{ include "druid.component.name" (list "broker" .) }}
  labels:
    {{- include "druid.component.labels" (list "broker" .) | indent 4 }}
  annotations:
    {{- include "druid.component.annotations" (list "broker" .) | indent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  {{- include "druid.service.ports" (list "broker" 8282) | nindent 2 }}
  selector:
    {{- include "druid.component.labels" (list "broker" .) | indent 4 }}
    {{- include "druid.component.match.labels" (list "broker" .) | nindent 4 }}
