apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: {{ include "druid.component.name" (list "broker" .) }}-nodepool
  labels:
    {{- include "druid.component.labels" (list "broker" .) | indent 4 }}
  annotations:
    {{- include "druid.component.annotations" (list "broker" .) | indent 4 }}
{{ include "druid.nodepool.spec" (list "broker" .) }}
