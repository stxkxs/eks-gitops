apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: {{ include "druid.component.name" (list "overlord" .) }}-nodepool
  labels:
    {{- include "druid.component.labels" (list "overlord" .) | indent 4 }}
  annotations:
    {{- include "druid.component.annotations" (list "overlord" .) | indent 4 }}
{{ include "druid.nodepool.spec" (list "overlord" .) }}
