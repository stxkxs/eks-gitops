# External DNS

serviceAccount:
  create: false
  name: external-dns

provider:
  name: aws

policy: sync

sources:
  - service
  - ingress

resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

tolerations:
  - key: CriticalAddonsOnly
    operator: Exists
