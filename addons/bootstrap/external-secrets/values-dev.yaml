# External Secrets - Dev overrides

replicaCount: 1

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::351619759866:role/dev-eks-external-secrets

webhook:
  replicaCount: 1

certController:
  replicaCount: 1
